requires 'perl', '5.020';

# The core runtime uses core Perl modules (POSIX, Scalar::Util) plus ONE
# CPAN dependency (#2344): DateTime::TimeZone, the tzdata source for
# named-IANA-zone resolution in `format_date` (loaded lazily — every other
# helper stays core-only). No template engine or web framework is pulled in
# here — those live in the backend dists (BarefootJS-Backend-Xslate,
# Mojolicious-Plugin-BarefootJS).
requires 'DateTime::TimeZone';

on 'test' => sub {
    requires 'Test2::V0';
};

on 'configure' => sub {
    requires 'Module::CPANfile';
};
