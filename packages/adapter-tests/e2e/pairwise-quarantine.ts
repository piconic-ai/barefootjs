/**
 * Pairwise-sweep quarantine ledger (#2481 step 5, browser-oracle leg).
 *
 * `pairwise.playwright.ts` runs the same three oracles `oracle.playwright.ts`
 * and `mutation.playwright.ts` run (`oracle-core.ts`), but against every
 * `status: 'ok'` case `scripts/pairwise-generate.ts` produced from the t=2
 * covering array. Mirrors `mutation-quarantine.ts`'s shape and rot-check
 * discipline exactly (see that file's docstring for the rationale): a bare
 * skip would go silently stale the moment a fix lands, so
 * `pairwise.playwright.ts` instead asserts each quarantined
 * `[caseId, oracle]` pair is STILL failing — a pair that starts passing
 * fails its rot check with a "stale — delete the entry" message.
 *
 * ONE deliberate difference from `mutation-quarantine.ts`: mutation has
 * `baseAlreadyQuarantined`, which skips a mutant's oracle when the SAME
 * oracle is already known-broken on the unmutated base fixture — pairwise
 * has no base fixture (every case is synthesized fresh from the covering
 * array), so there is no equivalent "already known broken" set to inherit
 * from and no analogous skip. Every pairwise oracle failure is a genuine
 * new finding.
 *
 * Key STRICTLY on the exact case id (the full axis-tuple string
 * `scripts/pairwise-generate.ts`'s `idFor` produces) — never on an axis
 * pattern or wildcard. If N cases fail from one root cause, that is N
 * entries sharing one `reason`/`issue`, not one entry matching all of
 * them: a pattern entry is how a quarantine quietly becomes a blanket
 * skip, which is exactly the failure mode this ledger exists to prevent
 * (CLAUDE.md's `known-limitation` discipline, applied here).
 *
 * Populated 2026-08-28 from a full sweep at head `1d1a8e8d7` (111 failing
 * `[caseId, oracle]` pairs). Triaged into nine `known-limitation` issues
 * (#2749–#2751, #2753–#2757, plus the pre-existing #2714) by reading each
 * failure's actual `Expected`/`Received` diff (or, for the browser-console
 * cases, a targeted diagnostic script), not by assuming the issue-mapping
 * table a human handed down in advance — see each `reason` for the
 * specific evidence. `assertSnapshotsAgree` (oracle-core.ts) runs the
 * structural-HTML `toBe` check before the DOM-state `toEqual` check inside
 * ONE call, so a case failing BOTH throws on the HTML check and never
 * reaches the DOM-state one — several entries below name a second,
 * currently-masked defect for exactly this reason (search "fires first").
 * Two rows (`early-return` + `controlled-select`, `state-prop-shadowing-
 * signal`, oracles `snap`/`three-point`) measured as a genuinely distinct,
 * un-diagnosed divergence (a controlled `<select>` whose value matches no
 * `<option>` resolves `selectedIndex`/`value` differently between SSR's
 * browser-default behavior and the client's explicit property write) that
 * does not fit any of the nine issues — deliberately left OUT of this
 * ledger rather than forced into the nearest bucket; that pair is still
 * red and reported separately as unquarantined.
 *
 * 2026-08-29 graduation: BF044 (`checkBareSignalOrMemoIdentifier`,
 * `jsx-to-ir.ts`) widened from a top-level-identifier-only check to a
 * recursive descent over rendered positions (a DOM element's attribute
 * value, a JSX text child — never a component prop, where an uncalled
 * getter is this codebase's deliberate Context-Provider idiom). Every
 * `state: getter-elided-signal` case this ledger quarantined under
 * #2755 (22 rows) and #2751 (8 rows) now gets refused at compile time
 * instead of silently misbehaving, so those cases dropped out of the
 * pairwise generator's `ok` set entirely and their entries were
 * deleted as stale — not "now passes this oracle" (the rot-check's own
 * phrasing), but "no longer exists to run the oracle against at all".
 * The SAME getter-elided-signal cases also carried #2753/#2754/#2714
 * entries (6/4/2 rows) for orthogonal defects (row `data-key`, a
 * dropped `data-pw-event` prop, a missing `data-mounted` under SSR) —
 * those went stale for the identical reason and were deleted too; the
 * issues themselves stay open for whichever `ok` cases still exercise
 * their mechanism. 97 pairwise cases, ok 85→62, refused 12→35 (all 23
 * newly-refused cases move BF023/BF044-refused, 0 broken throughout).
 *
 * 2026-08-29 graduation, #2756 and #2754. #2756: the client-side row /
 * branch builders (`irToHtmlTemplate` and its composite-row twin
 * `irToPlaceholderTemplate`, html-template.ts) now honour `attr.clientOnly`
 * the way the component and CSR template paths always did, so a rebuilt
 * `<textarea>`/`<select>` row no longer carries a literal `value` attribute
 * a hydration-reused row never had — 5 of the 6 rows graduated. The one
 * that stays is the INPUT sub-mechanism, the opposite direction (SSR bakes
 * `value="0"`, the client sets only the property, so a hydration-reused
 * `<input>` keeps an attribute a freshly built one never has); the issue
 * stays open for it. #2754: a `{...props}` forward now makes its host
 * element need a slot id, and `needsClientJs` counts the rest-attrs
 * application, so `applyRestAttrs` is emitted and addressable — all 16
 * rows graduated. The 6 `structure-child-component` idempotence rows that
 * still fail were RE-FILED under #2757, not kept: measured directly, both
 * legs now carry `data-pw-event="1"` and the click lands, and the only
 * remaining difference is the csr-mount scope-id prefix those cases'
 * `three-point` rows already record. 187 pairwise tests, 15 quarantined
 * rows deleted (10 under #2754, 5 under #2756) and 6 re-filed from
 * #2754 to #2757 — no #2754 row remains.
 *
 * 2026-08-29 graduation, #2749 and #2757. #2749: `collectReactiveChildProps`
 * (collect-elements.ts) now reads `classifyDOMProp` — the shared classifier
 * `applyRestAttrs` already used — instead of a hand-rolled `on[A-Z]` test, so
 * a `ref` on a child-component call site is no longer mirrored as
 * `setAttribute('ref', String(<fn source>))` by the emitted `init`. All 6 rows
 * were RE-FILED to #2714, not deleted: with the attribute leak gone the
 * callback runs, and what remains is that a `ref` cannot run during SSR at all
 * — the hydrated leg carries the `data-mounted` it sets and the SSR leg does
 * not, which is #2714's capability gap, measured directly on all three case
 * ids. #2757: `materializeComponent` (component.ts) now derives a scope id for
 * a top-level root-is-a-child-call wrapper purely to thread into
 * `_parentScopeId`, so the nested `renderChild` derives the child's `bf-s`
 * from the WRAPPER (`PairwiseCase_*_sN`) instead of from its own display name
 * (`PairwiseRow_*_sN`) — all 9 rows went stale and were deleted. 187 pairwise
 * tests, 9 rows deleted and 6 re-filed from #2749 to #2714 — no #2749 or
 * #2757 row remains.
 *
 * Correction while re-reading the rows this round removed: the six #2757
 * `idempotence` reasons stated the divergence backwards ("csr-mount reads
 * `PairwiseCase_*_sN` where SSR and hydration both produce
 * `PairwiseRow_*_sN`"). The `three-point` reasons, and #2757's own body, had
 * it right — csr-mount is the leg that names the child after itself. The
 * rows are deleted, so the wrong text goes with them; noted here so the
 * pattern (a reason transcribed with Expected/Received swapped, the same trap
 * #2714's own correction records) stays visible.
 *
 * 2026-09-02 graduation, #2756 fully closed. The INPUT sub-mechanism left
 * open by the 2026-08-29 graduation is fixed: `buildLoopSkeletonTemplate`'s
 * hoisted shared-`<template>` fast path now refuses to hoist a row carrying
 * a non-`clientOnly` property-only bind (`value`/`checked`, per the shared
 * `classifyDOMProp` classifier), falling back to the per-row interpolated
 * template that already bakes the attribute correctly, instead of omitting
 * it and trusting a `createEffect` that (by design, #2716) only ever writes
 * the DOM property, never `setAttribute`. All 21 quarantined `[caseId,
 * oracle]` rows under #2756 (the 1 surviving row from 2026-08-29, plus 10
 * newly-triaged cases × idempotence/three-point) re-measured as passing —
 * confirmed directly via `bunx playwright test e2e/pairwise.playwright.ts
 * -g controlled-input`, which also confirmed no row silently traded this
 * divergence for a different, previously-masked one (every non-quarantined
 * oracle for the same case ids passes too). All 21 rows deleted; #2756 has
 * no remaining rows in this ledger.
 */

import type { OracleKind } from './oracle-quarantine'

export interface PairwiseQuarantineEntry {
  caseId: string
  oracle: OracleKind
  /** Why — a short human summary of the observed divergence. */
  reason: string
  /** `known-limitation` issue URL, filled in after triage. */
  issue?: string
}

function key(caseId: string, oracle: OracleKind): string {
  return `${caseId}::${oracle}`
}

const ENTRIES: readonly PairwiseQuarantineEntry[] = [

  // --- #2753: fully graduated, no entries remain -----------------------
  // 系統2 root-cause fix (`IRElement.keyAttr`, see jsx-to-ir.ts): both
  // shapes graduated. 18 of the 24 rows went stale (the case now agrees on
  // the row-key attribute for every oracle exercised) and were deleted.
  // The 5 rows below survive the sweep but NOT for #2753 — each was
  // re-measured by un-quarantining it and reading the raw diff, and each
  // is re-attributed to the issue that actually keeps it red. Two distinct
  // mechanisms, both of which `assertSnapshotsAgree` could not reach while
  // the `data-key` difference sorted first (structural HTML compares once,
  // first-diff-wins):
  //   - 4 rows (2 caseIds x snap/three-point): the residue is #2714's
  //     ref-callback `data-mounted` stamp, which SSR cannot produce. The
  //     unkeyed `fragment-row-loop` now emits NO row-key attribute in
  //     either leg, which is the correct output for an unkeyed loop.
  //   - 1 row (three-point only): a second divergence that was genuinely
  //     hidden until now — #2750. A sixth row sat here for #2756 and
  //     graduated with that fix (2026-08-29).
  // Nothing here is filed under #2753 any more; a rot-check that still
  // found #2753 rows would be reading a stale ledger, which is the exact
  // failure this file exists to prevent.
  { caseId: "state-memo__structure-fragment-row-loop__binding-controlled-select__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "#2753's own divergence is fixed (this unkeyed `fragment-row-loop` now emits no row-key attribute in EITHER leg, which is the correct output for an unkeyed loop). What remains is the #2714 half alone, measured directly by un-quarantining this row: the sole structural difference is `data-mounted=\"0\"`, present in the hydrated leg and absent from SSR, because a ref-callback mount effect cannot run at SSR. No `data-key` appears in either leg.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-fragment-row-loop__binding-controlled-select__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "#2753's own divergence is fixed (this unkeyed `fragment-row-loop` now emits no row-key attribute in EITHER leg, which is the correct output for an unkeyed loop). What remains is the #2714 half alone, measured directly by un-quarantining this row: the sole structural difference is `data-mounted=\"0\"`, present in the hydrated leg and absent from SSR, because a ref-callback mount effect cannot run at SSR. No `data-key` appears in either leg.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment-row-loop__binding-class__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "#2753's own divergence is fixed (this unkeyed `fragment-row-loop` now emits no row-key attribute in EITHER leg, which is the correct output for an unkeyed loop). What remains is the #2714 half alone, measured directly by un-quarantining this row: the sole structural difference is `data-mounted=\"0\"`, present in the hydrated leg and absent from SSR, because a ref-callback mount effect cannot run at SSR. No `data-key` appears in either leg.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment-row-loop__binding-class__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "#2753's own divergence is fixed (this unkeyed `fragment-row-loop` now emits no row-key attribute in EITHER leg, which is the correct output for an unkeyed loop). What remains is the #2714 half alone, measured directly by un-quarantining this row: the sole structural difference is `data-mounted=\"0\"`, present in the hydrated leg and absent from SSR, because a ref-callback mount effect cannot run at SSR. No `data-key` appears in either leg.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-nested-loop-depth-2__binding-boolean-attr__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "#2753's own divergence is fixed (both legs now agree on `data-key`/`data-key-1`) — this row stays red for an UNRELATED, previously-masked reason `assertSnapshotsAgree`'s first-diff-wins structural compare never reached until now: this case's nested-loop `ref` const (`handleMount`) has its call site emitted but its declaration dropped from the module (#2750), so csr-mount throws `ReferenceError: handleMount is not defined` before rendering anything (hydrated: full markup; csr-mount: empty string) — confirmed directly (page console).", issue: "https://github.com/piconic-ai/barefootjs/issues/2750" },

  // --- #2714 ---------------------------------------------------------
  { caseId: "state-memo__structure-signal-array-loop__binding-controlled-input__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-signal-array-loop__binding-controlled-input__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-fragment__binding-style__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-fragment__binding-style__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-class__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-class__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-keyed-loop__binding-attr__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-keyed-loop__binding-attr__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-preamble-builder-body__binding-text__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-preamble-builder-body__binding-text__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },

  // --- #2714 (re-filed from #2749, which is fixed) ---------------------
  { caseId: "state-memo__structure-child-component__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "Fixed for the `ref` attribute half — #2749 is graduated: the client-JS mirror no longer writes the callback's source text into a `ref` DOM attribute (the emitted `init` used to run `__scope.setAttribute('ref', String(__v))` beside the correct `initChild` getter), and the callback now runs, so BOTH legs are free of `ref=` and the hydrated leg carries the `data-mounted` the callback sets. RE-FILED, not deleted: the row still fails, for a different and previously-masked reason. Measured directly — SSR renders no `data-mounted` at all while the hydrated leg has it, because a `ref` callback cannot run during SSR. That is #2714's capability gap (`ref`-computed attribute state never reaches SSR), not a silent divergence. #2749's `setAttribute('ref')` check fired first and masked it.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-child-component__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "Fixed for the `ref` attribute half — #2749 is graduated: the client-JS mirror no longer writes the callback's source text into a `ref` DOM attribute (the emitted `init` used to run `__scope.setAttribute('ref', String(__v))` beside the correct `initChild` getter), and the callback now runs, so BOTH legs are free of `ref=` and the hydrated leg carries the `data-mounted` the callback sets. RE-FILED, not deleted: the row still fails, for a different and previously-masked reason. Measured directly — SSR renders no `data-mounted` at all while the hydrated leg has it, because a `ref` callback cannot run during SSR. That is #2714's capability gap (`ref`-computed attribute state never reaches SSR), not a silent divergence. #2749's `setAttribute('ref')` check fired first and masked it.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-child-component__binding-text__event-ref-callback__callback-function-reference", oracle: "snap", reason: "Fixed for the `ref` attribute half — #2749 is graduated: the client-JS mirror no longer writes the callback's source text into a `ref` DOM attribute (the emitted `init` used to run `__scope.setAttribute('ref', String(__v))` beside the correct `initChild` getter), and the callback now runs, so BOTH legs are free of `ref=` and the hydrated leg carries the `data-mounted` the callback sets. RE-FILED, not deleted: the row still fails, for a different and previously-masked reason. Measured directly — SSR renders no `data-mounted` at all while the hydrated leg has it, because a `ref` callback cannot run during SSR. That is #2714's capability gap (`ref`-computed attribute state never reaches SSR), not a silent divergence. #2749's `setAttribute('ref')` check fired first and masked it.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-child-component__binding-text__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "Fixed for the `ref` attribute half — #2749 is graduated: the client-JS mirror no longer writes the callback's source text into a `ref` DOM attribute (the emitted `init` used to run `__scope.setAttribute('ref', String(__v))` beside the correct `initChild` getter), and the callback now runs, so BOTH legs are free of `ref=` and the hydrated leg carries the `data-mounted` the callback sets. RE-FILED, not deleted: the row still fails, for a different and previously-masked reason. Measured directly — SSR renders no `data-mounted` at all while the hydrated leg has it, because a `ref` callback cannot run during SSR. That is #2714's capability gap (`ref`-computed attribute state never reaches SSR), not a silent divergence. #2749's `setAttribute('ref')` check fired first and masked it.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-child-component__binding-class__event-ref-callback__callback-function-reference", oracle: "snap", reason: "Fixed for the `ref` attribute half — #2749 is graduated: the client-JS mirror no longer writes the callback's source text into a `ref` DOM attribute (the emitted `init` used to run `__scope.setAttribute('ref', String(__v))` beside the correct `initChild` getter), and the callback now runs, so BOTH legs are free of `ref=` and the hydrated leg carries the `data-mounted` the callback sets. RE-FILED, not deleted: the row still fails, for a different and previously-masked reason. Measured directly — SSR renders no `data-mounted` at all while the hydrated leg has it, because a `ref` callback cannot run during SSR. That is #2714's capability gap (`ref`-computed attribute state never reaches SSR), not a silent divergence. #2749's `setAttribute('ref')` check fired first and masked it.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-child-component__binding-class__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "Fixed for the `ref` attribute half — #2749 is graduated: the client-JS mirror no longer writes the callback's source text into a `ref` DOM attribute (the emitted `init` used to run `__scope.setAttribute('ref', String(__v))` beside the correct `initChild` getter), and the callback now runs, so BOTH legs are free of `ref=` and the hydrated leg carries the `data-mounted` the callback sets. RE-FILED, not deleted: the row still fails, for a different and previously-masked reason. Measured directly — SSR renders no `data-mounted` at all while the hydrated leg has it, because a `ref` callback cannot run during SSR. That is #2714's capability gap (`ref`-computed attribute state never reaches SSR), not a silent divergence. #2749's `setAttribute('ref')` check fired first and masked it.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },

  // --- #2757: fully graduated, no entries remain -----------------------
  // A top-level root-is-a-child-call component (`comment: true`,
  // `fragmentRoot: false`) now derives a scope id purely for threading into
  // `_parentScopeId` (`materializeComponent`, component.ts), so the nested
  // `renderChild` names the child after the WRAPPER — matching SSR and
  // hydration — instead of after itself. All 9 rows went stale and were
  // deleted.

  // --- #2750 ---------------------------------------------------------
  { caseId: "state-signal__structure-nested-loop-depth-2__binding-boolean-attr__event-ref-callback__callback-function-reference", oracle: "idempotence", reason: "Idempotence replays a click against the csr-mount leg; this case's nested-loop `ref` const (`handleMount`) has its call site emitted but its declaration dropped from the module (#2750), so both hydrate and csr-mount throw `ReferenceError: handleMount is not defined` — confirmed directly (page console on both legs). Only csr-mount actually loses its click target though (count=0): the hydrate leg's rows are still built by the template pass before the ref effect throws, so [data-pw-event] survives there and the click succeeds.", issue: "https://github.com/piconic-ai/barefootjs/issues/2750" },

  // --- #2758 ---------------------------------------------------------
  { caseId: "state-prop-shadowing-signal__structure-early-return__binding-controlled-select__event-direct-handler__callback-function-reference", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (prop `val: 7`) matches no option, so the browser defaults to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value. Measured pre = selectedIndex 0 / value 0, post = selectedIndex -1 / value empty. Independent of the harness `_p` handling: `bf-p` IS present in the served DOM, and `String(String(7))` and `String(String(undefined))` both match no option, so the divergence is identical either way (#2758).", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-early-return__binding-controlled-select__event-direct-handler__callback-function-reference", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (prop `val: 7`) matches no option, so the browser defaults to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value. Measured pre = selectedIndex 0 / value 0, post = selectedIndex -1 / value empty. Independent of the harness `_p` handling: `bf-p` IS present in the served DOM, and `String(String(7))` and `String(String(undefined))` both match no option, so the divergence is identical either way (#2758).", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },

  // --- #2481 t=3 sweep (variable-strength promotion, #2796/#2797) -----
  // Populated from the t=3 additionalCases browser-oracle sweep. All 156
  // failures below reduce to four pre-existing mechanisms plus one newly
  // filed one — none are a t=2-floor regression, each attributed by
  // reading the actual diff (or, where the symptom looked new, a targeted
  // headless-Chromium repro) rather than assumed from the axis combo alone.
  // A separate stale-`packages/client`-build artifact (39 cases crashing on
  // a missing `markupOrEmpty` export) inflated the raw sweep to 210 failures
  // before a rebuild dropped it to this real 156 — not entered here, since
  // it was never a product defect.

  // --- #2714 (ref-callback data-mounted, 59 new cases) -----------------
  { caseId: "state-memo__structure-child-component__binding-boolean-attr__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-child-component__binding-boolean-attr__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-child-component__binding-controlled-select__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-child-component__binding-controlled-select__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-component-row-root-loop__binding-text__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-component-row-root-loop__binding-text__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-fragment-row-loop__binding-text__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-fragment-row-loop__binding-text__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-fragment__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-fragment__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-keyed-loop__binding-controlled-select__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-keyed-loop__binding-controlled-select__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-keyed-loop__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-keyed-loop__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-keyed-loop__binding-controlled-textarea__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-keyed-loop__binding-controlled-textarea__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-nested-loop-depth-2__binding-style__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-nested-loop-depth-2__binding-style__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-preamble-builder-body__binding-class__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-preamble-builder-body__binding-class__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-signal-array-loop__binding-style__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-signal-array-loop__binding-style__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-static-array-loop__binding-boolean-attr__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-static-array-loop__binding-boolean-attr__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-static-array-loop__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-memo__structure-static-array-loop__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-child-component__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-child-component__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-component-row-root-loop__binding-controlled-select__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-component-row-root-loop__binding-controlled-select__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-conditional-ternary__binding-attr__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-conditional-ternary__binding-attr__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-conditional-ternary__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-conditional-ternary__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-early-return__binding-class__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-early-return__binding-class__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-early-return__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-early-return__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-fragment-row-loop__binding-attr__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-fragment-row-loop__binding-attr__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-fragment-row-loop__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-fragment-row-loop__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-keyed-loop__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-keyed-loop__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-nested-loop-depth-2__binding-class__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-nested-loop-depth-2__binding-class__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-preamble-builder-body__binding-class__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-preamble-builder-body__binding-class__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-preamble-builder-body__binding-controlled-textarea__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-preamble-builder-body__binding-controlled-textarea__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-preamble-builder-body__binding-style__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-preamble-builder-body__binding-style__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-signal-array-loop__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-signal-array-loop__binding-controlled-input__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-signal-array-loop__binding-controlled-select__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-signal-array-loop__binding-controlled-select__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-controlled-select__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-controlled-select__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-style__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-style__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-child-component__binding-style__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-child-component__binding-style__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-component-row-root-loop__binding-boolean-attr__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-component-row-root-loop__binding-boolean-attr__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-component-row-root-loop__binding-style__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-component-row-root-loop__binding-style__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-conditional-ternary__binding-class__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-conditional-ternary__binding-class__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-early-return__binding-attr__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-early-return__binding-attr__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment-row-loop__binding-boolean-attr__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment-row-loop__binding-boolean-attr__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment-row-loop__binding-style__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment-row-loop__binding-style__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment-row-loop__binding-text__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment-row-loop__binding-text__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment__binding-class__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-fragment__binding-class__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-keyed-loop__binding-attr__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-keyed-loop__binding-attr__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-keyed-loop__binding-text__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-keyed-loop__binding-text__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-nested-loop-depth-2__binding-attr__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-nested-loop-depth-2__binding-attr__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-nested-loop-depth-2__binding-attr__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-nested-loop-depth-2__binding-attr__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-signal-array-loop__binding-boolean-attr__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-signal-array-loop__binding-boolean-attr__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-static-array-loop__binding-text__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-prop__structure-static-array-loop__binding-text__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-component-row-root-loop__binding-class__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-component-row-root-loop__binding-class__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment__binding-boolean-attr__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment__binding-boolean-attr__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment__binding-text__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-fragment__binding-text__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-keyed-loop__binding-class__event-ref-callback__callback-filter-predicate", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-keyed-loop__binding-class__event-ref-callback__callback-filter-predicate", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-keyed-loop__binding-controlled-select__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-keyed-loop__binding-controlled-select__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-keyed-loop__binding-style__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-keyed-loop__binding-style__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-nested-loop-depth-2__binding-controlled-select__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-nested-loop-depth-2__binding-controlled-select__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-preamble-builder-body__binding-boolean-attr__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-preamble-builder-body__binding-boolean-attr__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-preamble-builder-body__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-preamble-builder-body__binding-controlled-input__event-ref-callback__callback-inline-arrow", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-signal-array-loop__binding-attr__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-signal-array-loop__binding-attr__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-signal-array-loop__binding-class__event-ref-callback__callback-sort-comparator", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-signal-array-loop__binding-class__event-ref-callback__callback-sort-comparator", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-static-array-loop__binding-controlled-textarea__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-static-array-loop__binding-controlled-textarea__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-static-array-loop__binding-style__event-ref-callback__callback-flatmap-callback", oracle: "snap", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },
  { caseId: "state-signal__structure-static-array-loop__binding-style__event-ref-callback__callback-flatmap-callback", oracle: "three-point", reason: "A `ref` callback's mount effect (`el.setAttribute('data-mounted', ...)`) never runs during SSR — same root cause as the existing accordion/radio-group/command entries in oracle-quarantine.ts (#2714): SSR bakes markup with no `data-mounted` attribute at all, and hydration's first effect pass adds it, so the SSR-vs-hydrated structural comparison sees an attribute appear that SSR never emitted.", issue: "https://github.com/piconic-ai/barefootjs/issues/2714" },

  // --- #2758 (out-of-range controlled-select, 6 new cases) -------------
  { caseId: "state-prop-shadowing-signal__structure-child-component__binding-controlled-select__event-handler-reading-outer-signal__callback-function-reference", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-child-component__binding-controlled-select__event-handler-reading-outer-signal__callback-function-reference", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-conditional-ternary__binding-controlled-select__event-direct-handler__callback-inline-arrow", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-conditional-ternary__binding-controlled-select__event-direct-handler__callback-inline-arrow", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-fragment-row-loop__binding-controlled-select__event-handler-reading-loop-param__callback-inline-arrow", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-fragment-row-loop__binding-controlled-select__event-handler-reading-loop-param__callback-inline-arrow", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-nested-loop-depth-2__binding-controlled-select__event-direct-handler__callback-function-reference", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-nested-loop-depth-2__binding-controlled-select__event-direct-handler__callback-function-reference", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-nested-loop-depth-2__binding-controlled-select__event-handler-reading-outer-signal__callback-filter-predicate", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-nested-loop-depth-2__binding-controlled-select__event-handler-reading-outer-signal__callback-filter-predicate", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-controlled-select__event-delegated-handler-in-row__callback-function-reference", oracle: "snap", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },
  { caseId: "state-prop-shadowing-signal__structure-static-array-loop__binding-controlled-select__event-delegated-handler-in-row__callback-function-reference", oracle: "three-point", reason: "SSR emits the select with no `selected` attribute because the bound value (a `prop-shadowing-signal` seeded to a value matching no option) makes the browser default to the first option; hydration then assigns `.value` explicitly, which for an out-of-range value yields selectedIndex -1 and an empty value — same mechanism as #2758 (originally found via `structure-early-return`), hit here through a different structure axis value.", issue: "https://github.com/piconic-ai/barefootjs/issues/2758" },

  // --- #2750 (nested-loop ref declaration dropped, 1 new case) ---------
  { caseId: "state-memo__structure-nested-loop-depth-2__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "idempotence", reason: "The nested-loop `ref` const (`handleMount`) has its call site emitted but its declaration dropped from the module (#2750, same mechanism as the existing nested-loop-depth-2 entry — different state/binding axis values, identical structure/event/callback) — confirmed directly via headless Chromium (`page.on('pageerror')`): `ReferenceError: handleMount is not defined` in both hydrate and csr-mount legs.", issue: "https://github.com/piconic-ai/barefootjs/issues/2750" },
  { caseId: "state-memo__structure-nested-loop-depth-2__binding-controlled-textarea__event-ref-callback__callback-function-reference", oracle: "three-point", reason: "The nested-loop `ref` const (`handleMount`) has its call site emitted but its declaration dropped from the module (#2750, same mechanism as the existing nested-loop-depth-2 entry — different state/binding axis values, identical structure/event/callback) — confirmed directly via headless Chromium (`page.on('pageerror')`): `ReferenceError: handleMount is not defined` in both hydrate and csr-mount legs.", issue: "https://github.com/piconic-ai/barefootjs/issues/2750" },

  // --- #2797 (component-row-root-loop handler declaration dropped, new issue, 2 cases) ---
  { caseId: "state-prop-shadowing-signal__structure-component-row-root-loop__binding-text__event-handler-reading-loop-param__callback-function-reference", oracle: "idempotence", reason: "A `component-row-root-loop` row's function-reference event-handler prop (`onClick: handleRowClick`) is referenced inside the `mapArray` row-construction callback but its declaration is dropped from the emitted module (#2797 — related to #2750's dropped-declaration class but a distinct code path: a single-depth loop whose row is a child-component call, not a nested ref-callback) — confirmed directly via headless Chromium: `ReferenceError: handleRowClick is not defined` thrown from `initPairwiseCase`, body.innerHTML ends up empty.", issue: "https://github.com/piconic-ai/barefootjs/issues/2797" },
  { caseId: "state-prop-shadowing-signal__structure-component-row-root-loop__binding-text__event-handler-reading-loop-param__callback-function-reference", oracle: "three-point", reason: "A `component-row-root-loop` row's function-reference event-handler prop (`onClick: handleRowClick`) is referenced inside the `mapArray` row-construction callback but its declaration is dropped from the emitted module (#2797 — related to #2750's dropped-declaration class but a distinct code path: a single-depth loop whose row is a child-component call, not a nested ref-callback) — confirmed directly via headless Chromium: `ReferenceError: handleRowClick is not defined` thrown from `initPairwiseCase`, body.innerHTML ends up empty.", issue: "https://github.com/piconic-ai/barefootjs/issues/2797" },
  { caseId: "state-prop__structure-component-row-root-loop__binding-class__event-handler-reading-loop-param__callback-function-reference", oracle: "idempotence", reason: "A `component-row-root-loop` row's function-reference event-handler prop (`onClick: handleRowClick`) is referenced inside the `mapArray` row-construction callback but its declaration is dropped from the emitted module (#2797 — related to #2750's dropped-declaration class but a distinct code path: a single-depth loop whose row is a child-component call, not a nested ref-callback) — confirmed directly via headless Chromium: `ReferenceError: handleRowClick is not defined` thrown from `initPairwiseCase`, body.innerHTML ends up empty.", issue: "https://github.com/piconic-ai/barefootjs/issues/2797" },
  { caseId: "state-prop__structure-component-row-root-loop__binding-class__event-handler-reading-loop-param__callback-function-reference", oracle: "three-point", reason: "A `component-row-root-loop` row's function-reference event-handler prop (`onClick: handleRowClick`) is referenced inside the `mapArray` row-construction callback but its declaration is dropped from the emitted module (#2797 — related to #2750's dropped-declaration class but a distinct code path: a single-depth loop whose row is a child-component call, not a nested ref-callback) — confirmed directly via headless Chromium: `ReferenceError: handleRowClick is not defined` thrown from `initPairwiseCase`, body.innerHTML ends up empty.", issue: "https://github.com/piconic-ai/barefootjs/issues/2797" },
]

export const PAIRWISE_QUARANTINE: ReadonlyMap<string, PairwiseQuarantineEntry> = new Map(
  ENTRIES.map(e => [key(e.caseId, e.oracle), e]),
)

export function pairwiseQuarantineEntry(caseId: string, oracle: OracleKind): PairwiseQuarantineEntry | undefined {
  return PAIRWISE_QUARANTINE.get(key(caseId, oracle))
}
