# Build output of build.sh (`cd component && bunx vite build` + `bun build`).
# Regenerated by ../build.sh; the committed artifact lives at
# site/core/public/slides/barefootjs-in-1min/assets/deck.js instead.
dist
bundle.js
