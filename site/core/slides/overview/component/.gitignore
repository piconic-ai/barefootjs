# vite's output, copied on to ../assets/ and public/slides/overview/assets/ by
# `bun run slides:build overview` (site/core/scripts/build-slides.ts). Both of
# those copies are gitignored in the repo root too.
dist
