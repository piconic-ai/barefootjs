---
"@barefootjs/jsx": minor
"@barefootjs/go-template": minor
---

Dynamic `.flat(depth)` and a widened `ParsedExpr` runtime evaluator, phase 1 (#2094, refs #2069).

- **Dynamic `.flat(depth)`**: a non-literal depth expression (a numeric prop, signal read, arithmetic, …) that itself resolves to a supported `ParsedExpr` is now accepted instead of refusing with BF101 — the depth is coerced at render time per JS `ToIntegerOrInfinity` (truncate toward zero; NaN / negative → `0`; `Infinity` / a huge finite value → flatten fully). The `array-method`/`flat` IR node gains an optional `depthExpr`; the shared `flatMethod` emitter interface widens to `FlatDepth | { expr: ParsedExpr }`. The Go runtime routes a dynamic depth through a NEW `bf_flat_dynamic`/`FlatDynamicDepth` helper — deliberately separate from the existing `bf_flat`/`Flat` (whose `-1` argument is a compile-time sentinel meaning "flatten fully", the opposite of what a genuinely dynamic `-1` means per JS). The other six template adapters wire up the same emit shape (calling their existing `bf_flat`-family helper with the rendered depth expression) so the interface compiles everywhere; their runtime coercion is a following phase.
- **Evaluator nested-callback widening**: the higher-order-callback runtime evaluator (`serializeParsedExpr`/`toEvalNode`) now serializes `.map(cb)` / `.filter(cb)` (1- or 2-param arrows, recursively — nesting is unbounded) and `.join(sep?)` instead of refusing them, so a predicate/projection body like `x => x.tags.filter(t => t.active).length > 0` or `posts.flatMap(p => p.tags.map(t => '#' + t)).join(' ')` now lowers on every template adapter instead of surfacing BF101. `.map`/`.filter` reuse the ordinary `call`/`member`/`arrow` node shape (not a bespoke wrapper), matching the shape the `eval-vectors.json` golden corpus already carries. A nested `.some`/`.find`/`.every`/`.sort`/`.reduce`/`.flat`/`.flatMap` inside a callback body is still refused. The Go runtime (`eval.go`) implements the new node shapes as the reference; the other five template adapters' evaluators haven't been ported yet (Phase 2) — a newly-serializable shape on those backends compiles clean but may not evaluate correctly at render time until their port lands.
- **`.flatMap(fn, thisArg)`**: the already-correct 2-arg form (the parser has always kept `thisArg`, and every adapter has always ignored it — arrows ignore `this`) is now pinned by a conformance fixture.

New conformance fixtures (`array-flat-dynamic-depth`, `array-flatmap-nested-map`, `array-flatmap-nested-filter-join`, `array-flatmap-thisarg`) pass on Go, CSR, and Hono; they are expected to fail on the other five template adapters until their evaluator ports land.
