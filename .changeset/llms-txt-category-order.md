---
"@barefootjs/cli": patch
---

`docs/core/llms.txt` (generated by `bf meta extract`) now lists chapters in the documentation's own order — Components, Reactivity, Rendering, Adapters, Advanced — and no longer reserves a slot for the removed `core-concepts` chapter.
